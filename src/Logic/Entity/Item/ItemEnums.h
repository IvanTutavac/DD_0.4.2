#pragma once

enum class WeaponType
{
	nothing, sword, axe, bow
};

enum class WeaponTypeEx
{
	nothing, shortSword, longSword, katana, shortBow, longBow
};

enum class WeaponAttackType
{
	nothing, normalSlash, stab, arrow, tripleArrow
};

enum class WeaponAttackDirection
{
	up, down, left, right
};
